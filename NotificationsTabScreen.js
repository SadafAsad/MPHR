import { StyleSheet, Text, SafeAreaView } from 'react-native';

const NotificationsTabScreen = () => {
    return (
        <SafeAreaView>
            <Text>Notifications Tab Screen</Text>
        </SafeAreaView>
    );
}
  
const styles = StyleSheet.create({
});

export default NotificationsTabScreen;
