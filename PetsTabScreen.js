import { StyleSheet, Text, SafeAreaView } from 'react-native';

const PetsTabScreen = () => {
    return (
        <SafeAreaView>
            <Text>Pets Tab Screen</Text>
        </SafeAreaView>
    );
}
  
const styles = StyleSheet.create({
});

export default PetsTabScreen;
