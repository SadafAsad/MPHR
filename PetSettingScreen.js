import { SafeAreaView, StyleSheet, Text } from 'react-native';

const PetSettingScreen = (navigation) => {
    return (
        <SafeAreaView>
            <Text>Pet Setting Screen</Text>
        </SafeAreaView>
    );
}
  
const styles = StyleSheet.create({
});

export default PetSettingScreen;