import { SafeAreaView, StyleSheet, Text } from 'react-native';


const AddVetClinicsScreen = (navigation) => {
    return (
        <SafeAreaView>
            <Text>Add Vet Clinics Screen</Text>
        </SafeAreaView>
    );
}
  
const styles = StyleSheet.create({
});

export default AddVetClinicsScreen;