import { SafeAreaView, StyleSheet, Text } from 'react-native';

const AddVetScreen = () => {
    return (
        <SafeAreaView>
            <Text>Add Vet Screen</Text>
        </SafeAreaView>
    );
}
  
const styles = StyleSheet.create({
});

export default AddVetScreen;