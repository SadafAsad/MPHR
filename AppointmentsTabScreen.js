import { StyleSheet, Text, SafeAreaView } from 'react-native';

const AppointmentsTabScreen = () => {
    return (
        <SafeAreaView>
            <Text>Appointments Tab Screen</Text>
        </SafeAreaView>
    );
}
  
const styles = StyleSheet.create({
});

export default AppointmentsTabScreen;
